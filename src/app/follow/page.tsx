"use client";

import { ComingSoonPage } from "@/components/layout/coming-soon";

export default function FollowPage() {
  return (
    <ComingSoonPage
      kicker="Continuity"
      title="Chat Follow"
      copy="Pick up a thread later, share a living conversation, and let Verxa keep context without forcing a new chat. This room is reserved."
    />
  );
}
